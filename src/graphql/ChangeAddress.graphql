mutation AccountAddressCreate($input: AddressInput!) {
  accountAddressCreate(input: $input) {
    address {
      id
      firstName
      lastName
      streetAddress1
      city
      postalCode
      country {
        code
        country
      }
    }
    errors {
      field
      message
    }
  }
}

 