mutation CustomerDesignFavoriteDelete($input: CustomerDesignFavoriteDeleteInput!) {
	customerDesignFavoriteDelete(input: $input) {
		errors {
			field
			message
		}
		result {
			message
			ok
			statusCode
		}
	}
}
