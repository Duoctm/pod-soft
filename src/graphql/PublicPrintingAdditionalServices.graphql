query PublicPrintingAdditionalServices($channel: String) {
	publicPrintingAdditionalServices(first: 1000, channel: $channel) {
		edges {
			node {
				id
				name
				price
				currency
				channel {
					id
					slug
				}
			}
		}
	}
}
