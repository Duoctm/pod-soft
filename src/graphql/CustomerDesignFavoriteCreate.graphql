mutation CustomerDesignFavoriteCreate($input: CustomerDesignFavoriteCreateInput!) {
	customerDesignFavoriteCreate(input: $input) {
		errors {
			field
			message
		}
		result {
			ok
			message
			statusCode
			favoriteId
		}
	}
}
