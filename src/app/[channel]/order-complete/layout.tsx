import React, { type ReactNode } from 'react'

const OrderCompleteLayout = ({ children }: { children: ReactNode }) => {
    return (
        <div>
            {
                children
            }
        </div>
    )
}

export default OrderCompleteLayout
